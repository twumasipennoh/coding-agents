# Calibration Lessons

Persistent record of how to pitch explanations for specific topics.
Written automatically by `/explain` on calibration misses; synced to
all projects via `sync-agents.sh`. Per-project additions go in
`<project>/.claude/calibration-local.md` (never synced).

Edit the global at `~/.claude/calibration.md`; vendored copies are
clobbered on sync.

**Positive companion:** this file records what *not* to do (prohibitions).
Its mirror is `~/.claude/references/voice-examples/` — kept replies to
*imitate*, keyed by deliverable shape and channel. Consult both: the
entry here for the rule, the exemplar there for a concrete landing.

---

### Investing / Finance
- **Wrong pitch**: contract/legal jargon, abstract instrument mechanics (pro-rata, dilution, MFN, conversion)
- **Right approach**: first principles with concrete numeric scenarios — dollar amounts, percentages, who-wins-who-loses for each clause
- **Learned**: 2026-05 — reviewing a SAFE, /explain on valuation cap terminology

### Git branching / history / state-recovery
- **Wrong pitch**: SHA identifiers, "common ancestor," "squash-merged under a different SHA," merge-strategy names, AND stash/pop/HEAD jargon ("stash pop conflict," "WIP captured while on main," "HEAD anomaly," "rebase target for the pop," "git stash pop --merge"). Also wrong: numbered lists of git commands as the plan itself (`git stash push -m "..."`, `git checkout main`, `git fetch origin --prune`, `git pull --ff-only`, `git branch -d`, `git stash pop`) — even a "plan" reply violates the rule when the commands ARE the plan instead of scenario-narrated actions.
- **Right approach**: scenario walkthrough for ALL git situations — sync AND state recovery AND cleanup plans. For sync: what does my local repo think? what does GitHub think? where did they split? what's the fix? Use the "two notebooks" mental model. For state recovery (stash/pop/HEAD movement): what's saved where? what's on the desk now? what was where before? what does the user do if they want X back? Use the "sealed envelope" / "save box" mental model — the saved work is set aside, user can throw it away, unpack carefully, or leave it sealed. For cleanup plans (`/merged`, post-merge branch delete): walk the steps as physical desk motions ("put these edits in a sealed envelope, switch your desk to point at main, pull down GitHub's latest, throw away the fix-branch label, unseal the envelope back onto the desk") — no `git` commands in the plan itself. Commands appear only when kwaku asks for the code-level version.
- **Learned**: 2026-06 — /explain after SHA-heavy diverged-history reply. Recurred 2026-06-23: branch cleanup plan used same jargon. Recurred 2026-06-30: /merged wrap-up used stash/pop/HEAD jargon extending into state-recovery territory. Recurred 2026-07-01: /merged PRE-cleanup plan used a numbered list of raw `git stash` / `checkout` / `pull --ff-only` / `branch -d` / `stash pop` commands as the plan itself. Same calibration rule applies to ALL git mechanics, not just sync — including pre-action plans, not just post-action reports.

### Claude reply composition / tool mechanics
- **Wrong pitch**: "final assistant turn," "mid-turn," "MCP stdio server," "registered in mcpServers," "PreToolUse hook" — named abstractions without grounding
- **Right approach**: step-by-step scenario from each actor's view — Claude composes → openclaw sees → telegram gets. Name abstractions only after the scenario has landed
- **Learned**: 2026-05 — /explain on cedit replacement discussion and MCP registration options

### openclaw / aide internals
- **Wrong pitch**: code paths, scanner functions, store names, parser internals ("ScheduleScanner.tick," "parseStreamJsonOutput," "LLMUnavailableError"), Map lookups, field-level guard conditions, line numbers. Also: describing feature-completion benefits in build terms ("parser patterns + executeIntent cases, half a day") instead of what the user gains. Also: writing dedup/uniqueness keys as tuples of internal field names like "(projectId, message, cron, nextFireAtUtcMs)"
- **Right approach**: open with the user-visible action or symptom, trace the chain in observable terms, THEN reference code paths. For tier/fallback systems, frame each tier as "what aide attempts on your behalf at this layer." For catalog/registry bugs, explain as "aide's directory had two entries with the same name, it grabbed the wrong one" not "entries.get() resolved the Smithery entry because !existing.transportKind was false." For "what does shipping X get me" questions AND bug-fix explanations, answer with scenarios: "you said X, aide did Y, here's why, here's what changes". For dedup/uniqueness rules, say plainly "same message at the same time = same reminder; same message at a different time = different reminder," and walk through what happens when the user repeats vs. extends a batch
- **Learned**: 2026-05 — multiple /explain invocations across pipeline, reminder, tier-fallback, and diagnostic-capability discussions. 2026-06 — Gmail OAuth mount bug diagnosed with Map keys and cleanup guard conditions instead of user-journey trace. 2026-06 — patch benefit + catalog seed bug both explained in implementation terms instead of user-experience scenarios. 2026-06 — evaluation risk/mitigation pitched as abstract system concern ("auto-scope mapping incomplete") instead of user failure story. 2026-06 — integration-runner crash diagnosis pitched as "discoverFromDir throws IntegrationRegistryError when pickIntegrationExport returns null" instead of "aide's helper crashes on startup because it trips over a file it shouldn't be loading". 2026-06 — http_request ad-hoc token fix described as "schema field + resolveAdHocAuth helper + credential-resolution gate" instead of "you paste a token, aide uses it for the request". 2026-06 — schedule-dedup batch behavior pitched as tuple-keyed dedup with `nextFireAtUtcMs` and `cron` field names instead of "same message at same time vs. same message at different time". 2026-06-29 — scheduled-task retry-cap root cause delivered with `pending-prompts.ts:2545`, `return null`, `Schedule.consecutiveFailures`, `AUTO_PAUSE_AT_CONSECUTIVE_FAILURES = 2`, `PendingPrompt`, `createSchedule` — instead of "you tell aide to remind you tomorrow at 9am; aide writes a record; scanner wakes every minute and fires due records; on failure it pings you and increments a counter; at 2 strikes it should auto-pause but doesn't, which is the bug." Investigator-agent structured spine output is mechanism-shaped by design — fine for the agent's body, but the parent skill's final delivery should still anchor in scenario when the user follows with /explain. 2026-06-29 — fix-options for the same retry-cap bug delivered with LoC counts, file:line refs, and guard-mechanism descriptions ("sanitize at the dispatcher structured-result paths, ~6 LoC" / "truncate at recordFailure's persist boundary, ~10 LoC") instead of "before: 9 pings every minute; after option A: 2 pings, second one says aide gave up, then silence; after A+B: same plus future-proofing." Fix-option presentation has the same before/after-scenario shape as bug diagnosis — list-of-options with implementation-surface descriptions is the wrong unit. Lead with user-visible behavior under each option, then the implementation surface only if asked. 2026-06-30 — sibling-sweep on the create_field N-yeses bug delivered as labeled bullet categories ("morning standup batch — 4 yeses", "pre-trip prep — 3 yeses", "two-person check-ins — 2 yeses"). kwaku invoked /explain "the scenarios for both that could be annoying." Labels name the bucket but not the felt experience. Right shape: a quoted telegram exchange — kwaku's actual typed message → aide's actual reply → the yes-yes-yes-yes click-through. The annoyance shape is the dialogue, not the category name. 2026-06-30 — same session, recommendation summary between options 1 and 2 for that same bug delivered as labeled reasons ("matches existing codebase pattern / smaller surface to ship / LLM-trust risk is bounded"). kwaku invoked /explain "with scenarios." Same shape miss: a category list is not a recommendation, it's a category list. The right shape for "why pick X over Y" is a quoted telegram exchange for the happy path under X, plus a second exchange for the failure path showing what kwaku sees when X's weakness manifests. Rule extends to all multi-option recommendation summaries. 2026-07-02 — fix-advocate diagnosis for a 6-target confirmedByUser hardening pass delivered as "`confirm: boolean` on 8+ intent shapes in `src/parser/intents.ts`... execute-intent.ts:2967... ~1200 LoC across ~25 files... Phase 1 ~350 LoC / Phase 2 ~400 / Phase 3 ~450." kwaku invoked /explain "these findings from a system perspective one turn at a time." Right shape for each finding: one user-facing scene — "you say X in a group thread, aide's brain decides Y, silently does Z, you never see the confirm" — plus the fix as "aide's brain can still ASK, but can't fake your yes." Rule extends to sibling-sweep enumerations (the extra 5 hiding places get one scenario each: "aide silently moves an item / deletes a memory / overwrites a template"), scope estimates (frame the size in the number of files you'd need to review, not raw LoC), and phasing recommendations (frame each phase by "what stops happening to you at the end of this PR"). LoC and file counts are footer material, not lead. 2026-07-02 — /investigate on the "aide-server tools aren't mounted" hallucination delivered with `classify-tier.ts:71-89`, `hasSeed:false`, `session-rotated-idle priorJsonlBytes:220885`, `getSkillsManifest`, `runAgentLoop`, `buildFullSystemPrompt`, and "Sonnet" as the LLM name in the lead. kwaku invoked /explain asking "couldn't the LLM search for and find the skill." Right shape for tool-hallucination bugs: **the receptionist analogy** — aide's brain got your message; there's a paging tool called `run_skill`; but aide's brain had no staff directory of saved skills, and no rule saying "check the directory before you deny paging." Also, the pre-processor that normally whispers "hey this looks like a skill" got tripped up by hyphen-vs-space in your phrasing. And 90 seconds of idle time had wiped the recent conversation, so no memory to fall back on. Cold brain + no whisper + no listed directory + no "check before deny" rule = confident, wrong story about the paging system being broken. Config values and file paths go in as annotations, and refer to the LLM as "aide's brain" (kwaku's frame), not "Sonnet." 2026-07-04 — /investigate on the "confirmation expired because service was restarted" reminders bug delivered with `applyInvalidationLine:497-501`, `openAideServerStores`, `pending-prompt-store.ts:283-288`, `DEC-197`, `F82`, `mintToken`, `metadata.confirmationToken`, and a table of file:line refs for each fix option. kwaku invoked /explain "this from user perspective." Right shape for confirmation-flow bugs where LLM emits and daemon resolves: lead with the yes-yes-yes tax as a quoted telegram exchange ("you type 4 reminders in one go → aide reads them back → you say yes → aide says 'expired, please yes again' → you yes → saves → next reminder → same again → 8 yeses for 4 reminders"). Then the analogy for the mechanism: **short-lived helper + always-on front desk sharing a file cabinet but with separate ticket books** — the helper (LLM-side aide-server subprocess) writes your confirmation slip and hands you a claim number from its own ticket book, then goes home; the front desk (daemon) later pulls the slip from the shared cabinet but can't find your claim number in its own ticket book, so it assumes a full reboot and issues a fresh slip. Numbers (2x reply tax) live in prose; file paths, DEC numbers, and process-boundary vocabulary do NOT appear in the recalibrated reply. 2026-07-04 — mid-`/fix` fix-advocate diagnosis for the aide-server MCP cold-start race delivered as 7 numbered steps citing `openAideServerStores`, `runAideServer:2275-2302`, `server.connect(transport):2301`, sequential-vs-`Promise.all`-by-dependency-level graphs, `~800ms → ~150-200ms` cold-start budgets, and "wins the probe race per anthropics/claude-code issue #60224." kwaku invoked `/explain before and after of this fix`. Right shape: two short telegram-thread scenes — BEFORE ("you send iPhone 16e for $575 to a project you haven't touched in a day; aide types Got it, then long silence, then a fallback text saying it can't save right now"; on the just-upgraded Claude Code binary "Got it → invisible retry → usually saves but sometimes still fails under load or when you have several projects cold-starting at once"), AFTER ("you send iPhone 16e for $575; aide types Got it, saves it cleanly on the first attempt every time, even when 10 projects are cold-starting simultaneously"). Rule: for **any** "before and after" / "what does this fix change" query, the deliverable is two brief telegram scenes only — no timing numbers, no file paths, no dependency graphs, no probe-race vocabulary. Those go in the follow-up "why is it faster" reply, only if kwaku asks. 2026-07-06 — /investigate on the acceptance-deploy-gate watcher that died silently; the recommended "detach + LLM continues the pipeline" option was pitched with `setsid`/SIGHUP/`longrun-tick.sh`/openclaw-cron/`agentTurn` mechanism. kwaku invoked /explain "the best option that will monitor and then the LLM continues the pipeline just like anything else." Right shape for openclaw session-continuation: a turn-by-turn telegram walkthrough — "you say deploy; the gate kicks the suite off in the background and your turn ends with a 'suite running, I'll pick it up when it's done' ack; ~3 min later the suite writes its result; a tiny always-on watcher notices and wakes a FRESH agent turn, handing it 'the suite you were waiting on finished, 46/46 green — continue the deploy'; that new agent reads the result and ships, then tells you 'deployed to staging.' From your side it looks like one agent that paused and resumed." The load-bearing idea to convey is that no single agent stays alive across the wait — the old turn ends, a new one is woken with the result — but say it as "paused and resumed," with setsid/cron/agentTurn as annotations only if asked. 2026-07-08 — /explain "what this means from a system and user perspective" on the poisoned-session /compact bug (skip-persist / MCP-tools-denied). Right shape: user scene ("you tell aide something to remember, say /compact, aide says 'Nothing to compact yet' as if nothing happened") + system scene ("in a brand-new workspace aide's brain wasn't told its saving tools are safe, so every save fails; a new safety rule then refuses to even remember which conversation it was") + stakes ("if it's the switched-off-tools case, every fresh install forgets every conversation, not just the test"). skip-persist/MCP/trust-dialog vocab is footer-only. 2026-07-12 — /explain "what these actually fix in terms of the functionalities" on a 3-fix menu for the openclaw long-run teardown, pitched by mechanism (setsid, print-mode exit, 600s Bash ceiling, sentinel). Right shape: one shared "what you live today" scene (kick off a test → "running, I'll report back" → result never comes → next session "it got torn down") + each fix as a before→after of that scene (normal run: result returns in one message; e2e batch: finishes fast instead of bundling a 27-min real-Claude test, which becomes a deliberate pre-deploy button; long job: keeps running detached, writes result to a file, I read it back next time — "paused and resumed"). Recurrence of the 2026-07-06 detach/continuation lesson; trigger is "explain the fixes functionally." When kwaku offers a candidate mental model ("does it basically do X?"), use confirm-and-refine, not re-explain. Warmup/handshake refinement that landed: two layers — cheap inventory (list the tools, gives the checklist, no LLM turn) vs one live execute-call (run one tool to prove it's reachable, not just listed); "listed ≠ usable" is the point; receptionist-first-morning analogy (glance at directory, then place one test call — a name can be listed while the line's dead). Trust-seed fix ("how this works to fix our issue"): building-access-badge analogy — the runtime treats a brand-new workspace as untrusted (guard won't let a new hire use the equipment until someone signs off the location is safe), so aide's tools were switched off and saves failed; fix = drop a pre-signed "trusted" note into the fresh workspace before the brain arrives, so tools work from second one; sign the one missing note rather than waving everyone through (bypass), since the subprocess is already fenced to its own tools. Config keys/flag names footer-only. 2026-07-12 — /investigate "why aide isn't running given our recent commits to keep the vm alive" delivered with `do_epoll_wait`, `worker_thread`, `ScheduleScanner.tick`, thread-ids, `kill -0`, "main event loop wedged." kwaku invoked /explain. Right shape (World A office/clerk): aide = the clerk who answers your messages; DEC-202 heartbeat = a pulse monitor strapped to the clerk's ASSISTANT, not the clerk; watchdog = a supervisor who walks by every 2 min and swaps in a fresh clerk if the pulse stops. Bug = the clerk froze at his desk but the assistant keeps tapping the pulse, so the supervisor sees a beating pulse and moves on (3h of unread messages). LEAD with the load-bearing distinction: this was NOT the building losing power (the VM-freeze the keepalive targets) — lights on all night, workers came and went, clock kept ticking, keepalive did its job — it's a different failure the pulse-on-the-assistant design can't see. Fix-now = swap in a fresh clerk (restart); real fix = move the pulse onto the clerk himself. Thread states/syscalls/timer names/DEC-202 are footer annotations. 2026-07-13 — /investigate root-caused a lost-confirmation flake (reminder "yes" rejected as `not-user-confirmed`); /explain "issue using before and after." I led with `resolveToken` / `registerPersistedTokenIfPresent` line refs / "in-memory only" / rehydrate-whiteboard / "6 stores." Right shape = two telegram scenes only — BEFORE (you say "remind me…", aide asks confirm, you say "yes", nothing lands / re-prompt, intermittently) / AFTER (same through "yes" → "Reminder created", every time). Recurrence of the 2026-07-04 before/after rule: a "before and after" ask is ALWAYS paired user-visible scenes, never mechanism; file paths / token-state vocab / store counts are follow-up-only.

### Internal API design
- **Wrong pitch**: leading with the proposed API shape, type signatures, or implementation mechanics. For **fix-scope / option tradeoffs**, also wrong: distinguishing the options by their *keying mechanism* ("predicate on `sideEffects.pendingPromptId`" vs "an allowlist `Set<Kind>`" vs "match the kind string") — that names how the code decides, not what the user gets.
- **Right approach**: lead with who calls the code and what they do with the return value, then frame changes as caller impact. For fix-scope option tradeoffs, define each option by its **consequences on the same 3-4 axes**: what actually gets fixed, what still stays broken, how much work, how risky. The keying mechanism is an annotation after the consequence, not the label of the option. Rule of thumb: the user should be able to choose without knowing the word "predicate."
- **Learned**: 2026-05 — feedback on internal-API change proposals. 2026-07-05 — /explain on the A/B/C keying tradeoffs for the aide schedule-cancel stop-line fix; options were labeled by their code mechanism (pendingPromptId predicate / allowlist Set / kind-string match) instead of by coverage + effort + risk.

### Test / CI infrastructure
- **Wrong pitch**: describing helper APIs, test-runner internals, config object shapes. Also wrong for **flaky-test fix-option menus**: leading each option with the config knob (`maxWorkers: 2`, `fileParallelism: false`, `execArgv: ['--import','tsx']`, `worker_threads`, "scheduler ticks") instead of what the option does to the flake.
- **Right approach**: developer-experience walkthrough — "you add X, here's what tomorrow looks like" — show what changes for the person writing tests, not how the plumbing works. For **flaky-test-from-contention** specifically, open with a crowded-shared-resource analogy (a small kitchen / one stove, N cooks each needing a timer to ding on time; when the stove is overloaded at a busy moment a timer gets ignored a few extra seconds and the dish is tossed — the recipe isn't wrong, the kitchen is too crowded). Then describe each fix option by **what it does to the flake**: "gives the fussy-timing tests their own quiet burner" (serial sub-project), "makes everyone cook one at a time — no ignored timers, but the meal takes 2-3 min longer" (full serial), "longer fuse on every timer so a briefly-ignored one still gets noticed" (bump timeout). Config keys are annotations after the plain-language effect, never the lead.
- **Learned**: 2026-05 — feedback on test-infra change explanations. 2026-07-13 — /explain on "how these options fix the test issue" (e2e awaitOutbox contention flake); option menu led with `maxWorkers`/`fileParallelism`/`execArgv` instead of the crowded-kitchen effect.

### Test fidelity / mock-vs-production accuracy
- **Wrong pitch**: describing the override mechanism ("we set AIDE_MOCK_JSONL_BYTES env var on the mock adapter")
- **Right approach**: three-section coverage walkthrough — (1) what's real (production code paths that fire), (2) what's faked (the single mocked input + why), (3) bug classes caught vs missed. Lead with the production code that's actually getting exercised; the mock is the asterisk, not the headline. One-line analogy ("crash-test ram vs real wall") AFTER the concrete sections, not in place of them.
- **Learned**: 2026-06 — F77/F78 acceptance scenarios: kwaku asked "how accurate?" after I described the env-var seam; the seam description didn't answer the coverage question.

### Test regression / test-infra failure explanations ("why did they break" / "why did the gate get bypassed")
- **Wrong pitch**: leading with the commit hash + the browser API that changed (IndexedDB, `page.route`, `onAuthStateChanged`, `requestAnimationFrame`), the test-runner internals (`useRealTimers`, `getByTestId`, `storageState`, `startHoldSeek`), or the setup-file name (`.playwright-auth.json`). Reader learns the mechanism but not what happened to their tests.
- **Right approach**: (1) pick one representative test and narrate the developer-run journey before-and-after in plain language — for regressions: "test navigates here, guard asks X, mock returned Y, guard let it through; now guard asks storage instead, mock can't reach it, test lands on login." For infra breakdowns: "setup script normally spins up a fake auth service, signs in a fake user, saves the login keychain to disk; this time the fake auth service never started, so the keychain was never written, so all 112 browsers opened cold and got kicked to login." For flaky-teardown gate bypasses: "these 6 tests need real time to elapse, flip a switch mid-test, then finish without turning the loop off; the loop keeps ticking after the test ends, and the runner marks the whole exit code as unclean." (2) per-layer viability + gap sweep (which layer catches what, which lost coverage, what's now dark), (3) name the missing alarm — why didn't we notice / why did the gate flip green when it should have flipped red. Gaps are the load-bearing part; the mechanism is annotation.
- **Learned**: 2026-07 — /explain after "132 Playwright auth failures" reply led with commit hash + IndexedDB terminology. 2026-07-03 — /explain on F46 bypassed-gates /investigate: reply led with `.playwright-auth.json`, `ProtectedRoute`, `useRealTimers`, `startHoldSeek`, `requestAnimationFrame`. Rule now explicitly covers infra breakdowns and gate-bypass explanations, not only regression walkthroughs.

### Cloud Run / scheduled function timeouts
- **Wrong pitch**: worst-case arithmetic ("5 users × 45s = 225s + init = 270s > 300s timeout"), function signatures, line numbers, `timeout_sec=300`
- **Right approach**: name the system actors and walk through what each one does — "Cloud Scheduler sends a request every 4 hours, Cloud Run spins up a container, the function checks Spotify then sends emails, Cloud Run kills it if it takes too long." Explain time pressure as a narrative, not a formula
- **Learned**: 2026-06 — /explain after two rounds of code-math-heavy timeout diagnosis

### Mobile keyboard / viewport interactions
- **Wrong pitch**: "blur the input," "debounce," "focus state," "scrollIntoView," "visualViewport API," "scroll-padding-bottom" — code-level mechanics
- **Right approach**: describe what's physically happening on screen — "the keyboard slides up and covers your results." Then what changes: "you stop typing, the keyboard drops on its own so you can see what matched." Walk through the motion under the user's thumb, not the DOM event chain
- **Learned**: 2026-06 — /explain on channel-picker search keyboard fix

### Design decisions / option presentation
- **Wrong pitch**: bundled numbered options that conflate independent choices ("Option 1: A+B+C, Option 2: A+D+E"). Also wrong: after listing axes + picks, ending with a vague drill-or-move-on prompt ("Want the next part, or drill in?") instead of naming the concrete action confirmation would trigger. Confirming a "pick" alone doesn't tell the user what gets done if they say "go."
- **Right approach**: present each design axis as a separate choice point — let the user mix and match independently. After listing axes + picks, write a short numbered list of the **concrete actions the picks would trigger** ("Confirm and I'll edit X to do Y, change Z to cite W, etc."). Close with "confirm / swap any pick / drill into one" — the user should be able to type "go" and know exactly what gets done. If picks don't translate to concrete edits, sharpen them first. **Same rule for meta-options / orchestration choices**: "all at once," "bundle with X," "defer to later," "run in parallel" imply an action just like picks do — unpack what each one physically changes (which files get edited once vs. twice, what infrastructure gets combined, what the net delta is) before offering it as a choice.
- **Learned**: 2026-05 — feedback on multi-axis design proposals. 2026-06-30 — /research on auto-calibration ended Q1 with 3 axes + 3 picks then asked "Want Q2, or drill into any Q1 axis?" Kwaku /explain'd asking what he was being asked to confirm. The picks implied actions but the actions were never spelled out. 2026-06-30 (same session) — offered "say 'all at once' to bundle with Q1" without naming what bundling meant. Kwaku /explain'd. Meta-options need the same unpacking as picks.

### Video player / iframe integration bugs
- **Wrong pitch**: naming YouTube IFrame Player API tokens (`cc_load_policy`, `loadModule`, `setPlaybackQuality`), React hook symbols (`useYouTubePlayer`, `handleReady`, `useState`), or file paths (`ContentCard.tsx`, `VideoOverlayControls.tsx`, `LibraryVideoModal`) in the diagnosis lead. Also wrong for historical-rationale / design-decision replies: leading with DEC-NNN numbers, F19 / F72 feature labels, or abstractions like "per-iframe state vs user-scoped state" / "user pref vs actual playback." Also wrong for post-fix engineering follow-up lists: leading with file paths (`TESTING_SUBTITLES_FIX.md`), code identifiers (`preferUnmuted`, `Tier-1 unmute`, `state=1 transition`), or telemetry metric names (`insem/client_error_player_prefs`). Even short bullet-list follow-ups violate the rule if each item opens with jargon before user impact.
- **Right approach**: walk what the user sees per video number and per tap. "You open the app. Video 1 starts. Captions are showing across the picture. But the CC button in the top-right? Off. Tap CC once — captions disappear. Tap again — back. The button works, but its starting state is a lie." Then explain the mechanism as annotation on the scenario, not as its lead. For persistence bugs specifically: "You set X on Video N. Swipe to Video N+1. X is gone." For "why is the code this way?" historical-rationale replies: same rule — walk the current behavior, walk the after behavior, then explain the old design's reasoning in plain English (not "DEC-128 says…" or "invalidated by F19's iframe-per-card contract"). For post-fix engineering follow-up lists: per item, lead with user-visible impact ("does the user see this today, and how?") + urgency ("worth a ticket / nice-to-have / doc hygiene"). File paths and code identifiers appear as annotations after the impact framing. DEC numbers and code names go in the eventual DEC entry, not in the recalibrated user-facing reply. Reserve iframe/API/hook vocabulary for a follow-up "want the code-level detail?" turn.
- **Learned**: 2026-07-01 — /explain on fix-advocate findings for the captions/speed/quality persistence bug (9th reinforcement in `feedback_bug_fix_explanations.md`). Reinforced same day, same session — /explain on the historical-rationale investigation reply (10th reinforcement). Reinforced again same session — /explain on the post-fix follow-ups bullet list that opened each item with file paths and code identifiers (11th reinforcement). Rule now covers three deliverable shapes: live-bug diagnoses, historical-rationale walkthroughs, and engineering follow-up lists.

### Problem-with-mitigations / options-list deliverables
- **Wrong pitch**: opening with the config flags, ring-buffer sizes, file paths, and field names that produce the failure (`compaction.mode: "safeguard"`, `session.maintenance.mode: "warn"`, "20-message ringBuffer", `~/.openclaw/openclaw.json:95`), then moving to the options list. Even if the config trace is technically correct, leading with it makes the user read mechanism before they can feel the failure. Also wrong: inventing concrete-feeling specifics (turn numbers, line ranges, quoted text) to give a scenario narrative texture. Internal contradictions surface immediately and destroy the whole explanation.
- **Right approach**: when the deliverable is "here is a broken thing and N ways to fix it," lead with a **user-journey scenario walkthrough of the root cause** — "you're in a long Telegram thread, every turn aide does X, eventually Y overflows, here's what you saw in your email-automation session at turn 165." Every specific number, quote, and config value in that scenario must come from actually-read evidence in this turn (the session JSON, the code, the agent report). If I don't have the specific, use a generic placeholder ("an earlier rotation event", "much earlier in the session"). Then list the options. Config flags and code paths can appear, but as **annotations on the scenario**, not as the lead. The options list itself follows one-beat-per-turn.
- **Also**: confirm which SYSTEM owns the config or behavior before citing it. aide (`~/projects/conversational-assistant/`, data at `~/.aide/data/`) and openclaw (`~/.openclaw/`) are TWO different systems with TWO separate compaction implementations. `~/.openclaw/openclaw.json` `compaction.mode: "safeguard"` controls openclaw's gateway-level pruning, not aide's per-project rotation. Aide's compaction lives in `~/projects/conversational-assistant/src/operations/session-compaction/` and is triggered independently at 75% of effective-window bytes.
- **Before/after ask (2026-07-07)**: when kwaku follows a mitigations list with "explain in before and after terms," recast each fix as a paired user-visible state — **Before** = what he lives through now (silent stall, having to poll "did it finish?"), **After** = what arrives on its own once it ships (a telegram message: done / stalled / failed). Mechanism name (Piece B auto-resume, stall-watchdog cron, self-ping at PR-open) is a trailing parenthetical, never the header. Triggered on a /research reply whose mitigations each led with their mechanism slug.
- **Learned**: 2026-06-30 — /research on aide compaction surfaced 6 fix options. Opened with a config-flag-laden "Root cause of the cliff" paragraph citing the wrong system's config, then dove into the options. kwaku invoked /explain "starting with the root cause before we think about mitigation options." Recalibrated to a scenario-style root cause that included invented turn numbers ("drops everything except turns 146-165. Turn 162, where Option A was named, is now gone") — internally contradictory since 162 is inside 146-165. kwaku flagged the math and the aide/openclaw conflation. Two separate failure modes the calibration now covers: scenario-must-be-grounded-in-evidence, and aide ≠ openclaw.

### Firebase Hosting rewrites / Cloud Function serving
- **Wrong pitch**: naming `firebase.json` rewrite paths (`/content/**`, `**` catch-all), `send_from_directory`, `current_app.static_folder`, `Flask(__name__)`, byte counts of the response body, or the fallback stub HTML source. Even opening with "Firebase Hosting sees the URL, matches the rewrite, forwards to the Cloud Function" leaks the mechanism before the user has felt the failure.
- **Right approach**: pure user-experience walkthrough of what the person tapping the link actually sees — "your friend taps the link, browser opens, screen goes white, no logo, no video, no error, no button. They wait, nothing loads, they swipe out and put the phone down. They don't tell you it's broken — they just move on." Then one plain-English sentence on the mechanism: "the servers were supposed to hand their phone the app; they handed it an empty envelope." Config file paths, function names, and byte counts go in a follow-up "want the code-level detail?" turn — never the lead.
- **Learned**: 2026-07-02 — /explain on the F46 /content/:id Flask stub bug (12th reinforcement in the bug-fix journey rule). Rule now explicitly covers infrastructure/routing/CDN failure explanations, not only React/iframe/video bugs.
- **Also covers serving-path CHANGES (not just failures)**: when explaining a proposed change to how the app is served for e2e (e.g. "run Flask directly instead of through the hosting+functions emulator"), lead with the traffic analogy — "how many lanes handle the requests" (one emulator lane = requests queue = flake; direct Flask = many lanes = no queue) — NOT "Werkzeug WSGI / functions-framework / threaded=True". Then ALWAYS split the impact into two explicit buckets kwaku cares about: **(a) does it change the real app / prod?** (usually "no — test-only launcher, prod runs on Google's real multi-lane server unchanged") and **(b) what changes in test infra?** (new launcher file, `MEDIATRACKER_BASE_URL` repoint, which emulators still needed, and the fidelity caveat that the direct server ≠ deployed function). — 2026-07-10, /explain on the "serve Flask directly for e2e" option.

### Instrumentation / audit-log health readouts
- **Wrong pitch**: leading with internal slug counts, env var names, and file:line refs when kwaku asks "how well is the tracker working?" — e.g. opening a rule-hits analysis with "`compact-oneliner` 15 / `compact-one-liner` 5 / `compact-oneliners` 2", "`CLAUDE_SESSION_ID` unset at [log-rule-hit.sh:38]", "rule_family drift includes `universal-output`, `feedback`". Reader learns the mechanism but not what's wrong with the readout they'd actually consult.
- **Right approach**: lead with the user-visible view first — what happens when kwaku types `/rule-status`, opens the dashboard, tails the log — and what's silently misleading about that view. "You type `/rule-status`, top rule looks like X with 15 hits; it's actually one rule under 5 spellings, so the leaderboard is wrong. Every row says session=unknown, so the 'how many sessions applied it' answer is always 1. The doc says 7 skills are opted in; real number is 14." Slug names, env var names, script paths appear as annotations after the user-visible miscount, not as the lead. Fix, when named, is framed as "next time you type `/rule-status` you'll see X instead of Y." Rule extends to any instrumentation-analysis reply: pipeline audit logs, rotation audit logs, rule-hits log, longrun-tick logs.
- **Learned**: 2026-07-03 — /explain "from the perspective of a user" after the lean-output tracker health analysis led with slug-count breakdown and helper script line numbers instead of "here's what you see when you type /rule-status, and here's what's wrong with it."

### Bug root-cause explanations / user-perspective diagnosis
- **Wrong pitch**: leading a bug diagnosis with the diagnostic-spine `Locate` section — file:line refs, function names, module vocabulary — before the user-visible symptom is described in plain terms. E.g. opening with "Route: `app.py:1578` `api_music_poll_now` → `_build_music_poller().poll_user_artists(...)`. Client seam: `polling_service.py:456` calls `get_artist_releases`…" Reader learns *where* the code lives but not *what misbehaved from the seat they were sitting in*.
- **Right approach**: lead with a user-journey walkthrough of the failure — what the user did (clicked Refresh All), what they saw (spinner, then empty dashboard), what should have happened, what actually happened, and why in plain terms ("the button uses an older path that was left on the old fast setting"). Code paths, `file:line`, function names appear only *after* the user-visible narrative, and only when the user explicitly asks for implementation detail. Overrides the diagnostic-spine template's `Locate` section for the user-facing reply — that section is scaffolding for internal analysis, not for the summary. For multi-item repeated failures, describe the pattern as perceived: "all five artists failed the same way," not "5 sequential 400s in 2s with lock-release after."
- **Learned**: 2026-06-25 — bug-fix reply confirmed "the number isn't updating" with function-name walkthrough → /explain. 2026-07-04 — investigator report on F62 Refresh-All 400s opened with a code-path-heavy Locate section (file:line refs across app.py / polling_service.py / client.py / registry.py / main.py); /explain "this from user perspective" recalibrated. 2026-07-10 — e2e failure diagnosis led with "cutoff isn't truncated to midnight" / "poll_tracked_artist returns an error" / "artists_skipped:5" → /explain "what this means from a user perspective." Three hits now — treat as settled: NEVER open a bug/test-failure diagnosis with mechanism; the plain-symptom walkthrough comes first, always. Applies across investigator, fix-advocate, ad-hoc diagnoses, and pipeline-failure post-mortems.

### Multi-part deliverable pacing / "continue" semantics
- **Wrong pitch**: two failure modes on the same rule. (a) **Dumping all parts at once** because "it's a single diagnostic deliverable." A "2 findings + 3 fix options + verdict" reply is a 4+ part answer, not one deliverable. Red-flag opener: "Two problems, not one" (also violates the CLAUDE.md antithesis ban). Red-flag section headers inside a diagnostic reply: `## Options`, `## Two problems`, `## Findings`, `## Fixes`, followed by bulleted alternatives. If I catch myself typing one of these mid-reply, stop and restructure — the header IS the tell. (b) **Replying "No response requested." (or silence)** to "continue" / "continue from where you left off" / "keep going" after I offered a part-1-of-N reply with a "continue?" prompt. Equally wrong: dumping the remaining N-1 parts in one turn once continue is requested.
- **Right approach**: before sending any diagnostic or advisory reply, count the distinct parts (findings, options, caveats, follow-ups). **If ≥3**, restructure: (1) lead with the single most load-bearing part (top-priority finding + its fix) in 1-3 sentences, (2) offer expansion in one short trailing phrase ("also a latent Flask bug + 3 fix options if you want them"), (3) stop. Do NOT use antithesis section headers — they announce the violation. When kwaku replies "continue" / "keep going" / "continue from where you left off," that IS the answer to my expansion offer — deliver the next single beat as its own turn. One option / one finding / one phase per turn. End with a "next is X, continue?" only if more parts remain; on the last part, just close. Bypass phrases for one-shot dump: "all at once" / "dump them all" / "skip." Trust kwaku to steer.
- **Learned**: 2026-06 — /research on aide compaction surfaced 6 options. Delivered Option 1 with "continue with options 2-6?"; kwaku replied "Continue from where you left off"; I replied "No response requested.", silently dropping the deliverable. /explain "with one turn at a time" recalibrated. 2026-07-02 — diagnostic reply on /content/:id "why login prompt?" opened with "Two problems, not one" and dumped 2 findings + 3 fix options + verdict in a single ~500-word turn. kwaku asked "Why did i get this large output" — the diagnosis WAS the rule miss. Rule now covers both upstream (dumping N parts at once, before user asks for continue) and downstream (silently dropping on "continue") failures. Applies across /research, /security-check, /design-check, /memory-review, /feature-status, /investigate, ad-hoc root-cause diagnoses, and fix-advocate delivery.
